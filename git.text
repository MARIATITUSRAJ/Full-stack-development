echo "# TITUS-REPO" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/MARIATITUSRAJ/TITUS-REPO.git
git push -u origin main

git config --global user.name "MARIATITUSRAJ"
git config --global user.email "titusraj30@gmail.com"
